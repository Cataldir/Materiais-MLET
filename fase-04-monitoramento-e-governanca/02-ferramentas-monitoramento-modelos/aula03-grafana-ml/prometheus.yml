global:
  scrape_interval: 15s

scrape_configs:
  - job_name: local_ml_metrics
    static_configs:
      - targets:
          - localhost:9090
        labels:
          source: local_snapshot

rule_files: []